# TRUSTNEXUS AI - Backend Environment Configuration

# Server Port (Render sets this automatically in production via $PORT)
PORT=8000

# Database URL (Defaults to local SQLite if unset)
DATABASE_URL=sqlite:///./trustnexus.db

# Allowed Frontend Origins (Comma-separated for multiple origins)
FRONTEND_URL=https://trustnexusai.netlify.app
